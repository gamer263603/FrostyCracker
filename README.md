# Aircrack-ng GUI Tool

A professional graphical user interface for the aircrack-ng wireless network auditing suite.

**Created by: CodeWithTed**

## 📦 Installation & Setup (Complete Guide)

### Step 1: Install Aircrack-ng Suite
```
sudo apt-get update
sudo apt-get install aircrack-ng
```

### Step 2: Install Python & PyQt5
```bash
pip3 install PyQt5
Or on Ubuntu/Debian:

bash
sudo apt-get install python3-pyqt5
```
### Step 3: Download the Application
Save the aircrack_gui.py file to your computer


### Step 4: Run the Application

```bash
sudo python3 aircrack_gui.py
🚀 How to Use (Quick Start)
Enable Monitor Mode

Go to "Monitor Mode" tab → Click "Refresh Interfaces" → Select your interface → Click "Enable Monitor Mode"

Scan Networks

Go to "Capture Packets" tab → Select interface → Click "Scan Networks" → Wait 30 seconds

Capture Packets

Enter filename (e.g., capture-01.cap) → Select channel → Click "Start Capture" → Click "Stop Capture" when done

Crack Password

Go to "Crack Password" tab → Select .cap file → For WPA: select wordlist → Click "Crack WEP" or "Crack WPA/WPA2"

⚠️ Legal Disclaimer
Only use on networks you own or have permission to test!

Created by CodeWithTed

text

This puts all the installation and usage steps in one clean, easy-to-follow section!
put steps in one markdown bro, i see you are seperating i just want to copyonce
Here's the complete README in one single markdown block:

```


# Aircrack-ng GUI Tool

A professional graphical user interface for the aircrack-ng wireless network auditing suite.

**Created by: CodeWithTed**

## Complete Installation & Usage Guide

### 1. Install Aircrack-ng Suite
```bash
sudo apt-get update
sudo apt-get install aircrack-ng
```
### 2. Install PyQt5
```
bash
pip3 install PyQt5
Or on Ubuntu/Debian:

bash
sudo apt-get install python3-pyqt5
```
### 3. Save the Application
```
Save the aircrack_gui.py file to your computer
```
### 4. Run the Application
```
bash
sudo python3 aircrack_gui.py
```
### 5. Using the Tool
```
Enable Monitor Mode: Go to "Monitor Mode" tab → Click "Refresh Interfaces" → Select interface → Click "Enable Monitor Mode"

Scan Networks: Go to "Capture Packets" tab → Select interface → Click "Scan Networks" → Wait 30 seconds

Capture Packets: Enter filename (e.g., capture-01.cap) → Select channel → Click "Start Capture" → Click "Stop Capture"

Crack Password: Go to "Crack Password" tab → Select .cap file → For WPA: select wordlist → Click "Crack WEP" or "Crack WPA/WPA2"

Requirements
Linux OS (Kali, Ubuntu, Debian)

Wireless adapter that supports monitor mode

Root/sudo privileges

Legal Disclaimer
Only use on networks you own or have permission to test! Unauthorized use is illegal.

Created by CodeWithTed🤖
```
